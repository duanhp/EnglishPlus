program EnglishPlus;

uses
  Forms,
  uMain in 'uMain.pas' {Form1},
  uSearchBar in 'uSearchBar.pas' {FmSearchBar},
  uSearchList in 'uSearchList.pas' {FmSearchList};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Englist+';
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TFmSearchBar, FmSearchBar);
  Application.CreateForm(TFmSearchList, FmSearchList);
  Application.Run;
end.
